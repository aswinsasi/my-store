"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateRequest = void 0;
const express_validator_1 = require("express-validator");
const request_validation_error_1 = require("../errors/request-validation-error");
const validateRequest = (req, res, next) => {
    const errors = (0, express_validator_1.validationResult)(req);
    if (!errors.isEmpty()) {
        throw new request_validation_error_1.RequestValidationError(errors.array());
    }
    next();
};
exports.validateRequest = validateRequest;
//  What This Middleware Does:
// It sits after your validation rules (like check() or body()).
// It catches any validation errors.
// If errors exist, it throws a standardized RequestValidationError.
// If everything is valid, it lets the request proceed via next().
