import { NextFunction, Request, Response } from "express";
declare global {
    interface Req extends Request {
        session?: any;
        currentUser?: any;
    }
}
export declare const currentUser: (jwt_key: string) => (req: Req, //Req u can use when custom Req
res: Response, next: NextFunction) => void;
