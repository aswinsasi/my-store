"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.currentUser = void 0;
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const currentUser = (jwt_key) => {
    return (req, res, next) => {
        var _a;
        if (!((_a = req.session) === null || _a === void 0 ? void 0 : _a.jwt)) {
            return next(); // ✅ Add return here
        }
        const jwt_key_value = jwt_key !== null && jwt_key !== void 0 ? jwt_key : process.env.JWT_KEY;
        try {
            const payload = jsonwebtoken_1.default.verify(req.session.jwt, jwt_key_value);
            req.currentUser = payload;
        }
        catch (err) {
            return next(err); // optional: return here for consistent flow
        }
        next();
    };
};
exports.currentUser = currentUser;
