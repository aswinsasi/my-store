"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.currentUser = void 0;
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const currentUser = (jwt_key) => {
    return (req, //Req u can use when custom Req
    res, next) => {
        var _a, _b;
        if (!((_a = req.session) === null || _a === void 0 ? void 0 : _a.jwt)) {
            next();
        }
        const jwt_key_value = jwt_key !== null && jwt_key !== void 0 ? jwt_key : process.env.JWT_KEY;
        try {
            const payload = jsonwebtoken_1.default.verify((_b = req.session) === null || _b === void 0 ? void 0 : _b.jwt, jwt_key_value); // no need Jwt payload when custom Req used
            req.currentUser = payload;
        }
        catch (err) {
            next(err);
        }
        next();
    };
};
exports.currentUser = currentUser;
